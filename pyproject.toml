[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "orvo"
version = "0.0.1"
description = "Performance regression testing for machine learning."
readme = "README.md"
requires-python = ">=3.10"

authors = [
  { name = "Ethan Kemeny", email = "ethan.kemeny1@gmail.com" }
]

[project.scripts]
orvo = "orvo.cli:main"